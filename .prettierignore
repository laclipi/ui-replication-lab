# Build artifacts and dependencies — Prettier should never touch these
node_modules
.next
out
build
coverage
.vercel

# Generated by Next on dev/build — owned by the framework
next-env.d.ts

# Lockfiles — formatters break their internal layout; let the package manager own them
package-lock.json
pnpm-lock.yaml
yarn.lock

# Markdown changelog/snapshots that auto-generate elsewhere (keep room here for the future)
CHANGELOG.md
